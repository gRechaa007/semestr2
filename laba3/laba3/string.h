#pragma once
#include<stdio.h>
#include<stdlib.h>

int stringSize(char* string);
int subString(char* string, char* sub);
int countSubStr(char* string, char* sub);
char splitStr(char* string, char sep);