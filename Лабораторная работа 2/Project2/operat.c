#include "operat.h"

Matrix matrix(char* fileName)
{
	/*FILE* file;
	
	int number;
	char tempChar = 0;
	Matrix resultMatrix;
	resultMatrix.matrix[0] = (int**)malloc(sizeof(int*));
	resultMatrix.matrix[0][0] = (int*)malloc(sizeof(int));
	int i = 0, j = 0;

	if (fopen_s(&file, fileName, "r") != 0)
		perror("");
	while (fscanf_s(file, "%d", &resultMatrix.matrix[i][j]) != EOF)
	{
		j++;
		resultMatrix.matrix[i] = (int*)realloc(resultMatrix.matrix[i], (j + 1) * sizeof(int));
		tempChar = fgetc(file);
		if (tempChar == '\n') {
			i++;
			resultMatrix
		}

	}*/

}
